export function registrationFormPlugin() {
  return {
    name: "skillproof-registration-form",
    enforce: "pre",
    transform(code, id) {
      if (!id.endsWith("/src/App.jsx")) return null;
      let next = code.replace(/onRegister=\{\(\) => setScreen\("register"\)\}/, 'onRegister={() => { setStudent({ ...emptyStudent }); setScreen("register"); }}');
      const start = next.indexOf("function Register(");
      const end = next.indexOf("\nfunction CollegeLogin", start);
      if (start < 0 || end < 0) return next === code ? null : { code: next, map: null };
      const replacement = `function Register({ student, updateStudent, onRegister, onLogin }) {
  const [collegeIdSearch,setCollegeIdSearch]=useState("");
  const [registeredColleges,setRegisteredColleges]=useState([]);
  const [showColleges,setShowColleges]=useState(false);
  const [departmentSearch,setDepartmentSearch]=useState("");
  const [showDepartments,setShowDepartments]=useState(false);
  const [selectedCareers,setSelectedCareers]=useState([]);
  const [careerSearch,setCareerSearch]=useState("");
  const [showCareers,setShowCareers]=useState(false);
  const departments=["Computer Science and Engineering","Computer Engineering","Civil Engineering","Chemical Engineering","Computer Applications","Computer Science","Computer Science and Business Systems","Computer Science and Design","Cyber Security","Data Science","Electrical and Electronics Engineering","Electronics and Communication Engineering","Electronics and Instrumentation Engineering","Information Technology","Information Science and Engineering","Mechanical Engineering","Mechatronics Engineering","Automobile Engineering","Aeronautical Engineering","Artificial Intelligence and Data Science","Artificial Intelligence and Machine Learning","Biotechnology","Biomedical Engineering","Production Engineering","Industrial Engineering"];
  const loadColleges=async()=>{if(registeredColleges.length)return;try{const d=await api("/students/colleges");setRegisteredColleges(d.colleges||[])}catch{setRegisteredColleges([])}};
  const q=collegeIdSearch.trim().toLowerCase();
  const matches=q?registeredColleges.filter(c=>(String(c.name)+" "+String(c.college_id)).toLowerCase().includes(q)).slice(0,8):[];
  const chooseCollege=(c)=>{setCollegeIdSearch(c.college_id||"");updateStudent("college",c.name);updateStudent("collegeId",c.college_id||"");setShowColleges(false)};
  const dq=departmentSearch.trim().toLowerCase();
  const departmentMatches=dq?departments.filter(d=>d.toLowerCase().startsWith(dq)):departments;
  const chooseDepartment=(d)=>{setDepartmentSearch(d);updateStudent("department",d);setShowDepartments(false)};
  const selected=selectedCareers;
  const filteredCareers=careers.filter(c=>!selected.includes(c)&&c.toLowerCase().includes(careerSearch.trim().toLowerCase()));
  const toggleCareer=(c)=>{const n=selected.includes(c)?selected.filter(x=>x!==c):selected.length<3?[...selected,c]:selected;setSelectedCareers(n);updateStudent("careers",n);setCareerSearch("");setShowCareers(false)};
  const valid=student.name&&student.email&&student.password&&student.collegeId&&student.college&&student.collegeEmail&&student.department&&student.graduationYear&&selected.length>0;
  return <Auth wide title="Create your profile" subtitle="Use your registered College ID so your institution can track your verified progress."><form onSubmit={e=>{e.preventDefault();onRegister(e)}}><SectionTitle title="Personal information" number="01"/><div className="grid md:grid-cols-2 gap-4"><Field label="Full name" value={student.name} onChange={v=>updateStudent("name",v)} placeholder="Your name"/><Field label="Personal email" value={student.email} onChange={v=>updateStudent("email",v)} placeholder="you@example.com" type="email"/><Field label="Password" value={student.password} onChange={v=>updateStudent("password",v)} placeholder="Create a password" type="password"/><Field label="College email ID" value={student.collegeEmail||""} onChange={v=>updateStudent("collegeEmail",v)} placeholder="you@college.edu" type="email"/></div><div className="mt-5 relative"><label className="block text-sm font-semibold text-slate-700 mb-2">Registered College ID</label><input required value={collegeIdSearch} onFocus={()=>{loadColleges();setShowColleges(true)}} onChange={e=>{const value=e.target.value;setCollegeIdSearch(value);setShowColleges(true);if(value.trim().toUpperCase()==="COL-PANIMALA-153913"){updateStudent("collegeId","COL-PANIMALA-153913");updateStudent("college","Panimalar Engineering College")}else{updateStudent("collegeId",value);updateStudent("college","")}}} placeholder="COL-PANIMALA-153913" className="w-full h-12 rounded-xl border border-slate-200 bg-white px-4"/>{showColleges&&q&&<div className="absolute z-30 left-0 right-0 mt-2 max-h-72 overflow-y-auto rounded-xl border border-slate-200 bg-white shadow-xl">{matches.map(c=><button key={c.college_id} type="button" onMouseDown={e=>e.preventDefault()} onClick={()=>chooseCollege(c)} className="w-full text-left px-4 py-3 hover:bg-teal-50"><p className="font-semibold">{c.name}</p><p className="text-xs text-teal-700">College ID: {c.college_id}</p></button>)}</div>}</div><div className="mt-3 p-3 rounded-xl bg-teal-50 border border-teal-100"><p className="text-sm font-bold text-slate-800">{student.college||"College not linked yet"}</p><p className="text-xs text-teal-700 mt-1">{student.collegeId||"Enter your registered College ID"}</p></div><div className="grid md:grid-cols-2 gap-4 mt-4"><div className="relative"><label className="text-sm font-semibold text-slate-700">Department</label><input required value={departmentSearch} onFocus={()=>setShowDepartments(true)} onChange={e=>{setDepartmentSearch(e.target.value);updateStudent("department",e.target.value);setShowDepartments(true)}} placeholder="Computer Science" className="w-full h-12 mt-2 rounded-xl border border-slate-200 bg-white px-4"/>{showDepartments&&<div className="absolute z-30 left-0 right-0 mt-2 max-h-64 overflow-y-auto rounded-xl border border-slate-200 bg-white shadow-xl">{departmentMatches.map(d=><button key={d} type="button" onMouseDown={e=>e.preventDefault()} onClick={()=>chooseDepartment(d)} className="w-full text-left px-4 py-3 text-sm hover:bg-teal-50 border-b border-slate-50">{d}</button>)}</div>}</div><Field label="Graduation year" value={student.graduationYear} onChange={v=>updateStudent("graduationYear",v)} placeholder="2027" type="number"/></div><SectionTitle title="Target careers" number="02"/><p className="text-sm text-slate-500">Select up to 3 target careers.</p><div className="relative mt-4"><div className="min-h-14 w-full rounded-xl border border-slate-200 bg-white p-2 flex flex-wrap gap-2">{selected.map(c=><span key={c} className="inline-flex items-center px-3 py-1.5 rounded-full bg-teal-50 border border-teal-200 text-teal-800 text-sm">{c}<button type="button" onClick={()=>toggleCareer(c)} className="ml-2 font-bold">×</button></span>)}{selected.length<3&&<input value={careerSearch} onFocus={()=>setShowCareers(true)} onChange={e=>{setCareerSearch(e.target.value);setShowCareers(true)}} placeholder="Search target careers..." className="flex-1 min-w-48 h-9 outline-none text-sm"/>}</div>{showCareers&&selected.length<3&&<div className="absolute z-30 left-0 right-0 mt-2 max-h-72 overflow-y-auto rounded-xl border border-slate-200 bg-white shadow-xl">{filteredCareers.map(c=><button key={c} type="button" onMouseDown={e=>e.preventDefault()} onClick={()=>toggleCareer(c)} className="w-full text-left px-4 py-3 text-sm hover:bg-teal-50">{c}</button>)}</div>}</div><button disabled={!valid} className="w-full h-12 mt-6 rounded-xl bg-slate-900 text-white font-semibold disabled:opacity-40">Create SkillProof profile</button></form><p className="text-center text-sm text-slate-500 mt-6">Already have an account? <button type="button" onClick={onLogin} className="text-teal-700 font-semibold">Sign in</button></p></Auth>;
}`;
      next = next.slice(0,start) + replacement + next.slice(end);
      return { code: next, map: null };
    },
  };
}
